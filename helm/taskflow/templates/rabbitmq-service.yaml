apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  labels:
    {{- include "taskflow.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5672
      targetPort: 5672
      protocol: TCP
  selector:
    {{- include "taskflow.selectorLabels" . | nindent 4 }}
    app: rabbitmq
