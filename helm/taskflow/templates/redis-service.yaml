apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    {{- include "taskflow.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    {{- include "taskflow.selectorLabels" . | nindent 4 }}
    app: redis
